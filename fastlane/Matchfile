git_url(ENV["MATCH_GIT_URL"])
storage_mode("git")

type("appstore")
app_identifier([ENV["APP_IDENTIFIER"] || "com.abidkhan.globalprayertimes"])

# In CI we never want match to try to create new certs — fail fast instead.
readonly(true) if ENV["CI"]
